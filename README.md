# Bankruptcy Forecasting Pipeline (RFSD-based)

## 📌 Назначение проекта

Данный репозиторий представляет собой **исследовательский и прикладной прототип пайплайна подготовки данных и обучения ML-моделей для прогнозирования банкротства предприятий РФ** на основе бухгалтерской (финансовой) отчётности.

Проект ориентирован на работу с **крупными массивами структурированных данных**, в том числе с использованием:

- хранилища 🤗 Hugging Face Datasets (набор `irlspbru/RFSD`);
- форматов `parquet` и `csv`;
- in-memory и lazy-processing подходов;
- SQL-движка DuckDB;
- современных ансамблевых ML-моделей.

Репозиторий демонстрирует **полный цикл**:
> от загрузки и валидации исходных данных →  
> через формирование обучающей выборки →  
> к обучению и анализу моделей машинного обучения.

---

## 🧠 Что здесь делается (high-level)

В рамках проекта реализованы следующие ключевые этапы:

1. Контролируемая загрузка и описание исходных данных  
2. Очистка и формирование экспертной разметки (банкрот / не банкрот)  
3. Исключение нерелевантных наблюдений на основании нормативных критериев  
4. Feature engineering с использованием временных лагов  
5. Подготовка обучающей выборки  
6. Обучение и сравнение ML-моделей  
7. Анализ результатов и визуализация  

---

## 🗂 Минимальная файловая структура проекта

### 🔹 Загрузка и первичная работа с данными RFSD

Загрузка и демонстрация минимальной работы с файлами из хранилища  
🤗 **Hugging Face Datasets** (`irlspbru/RFSD`)

- `./get_base_2.py`  
  Контролируемый процесс загрузки данных с последующим созданием файла-описания структуры и состава хранимых сведений.

- `./use_base_2.py`  
  Пример работы с файлами формата `.parquet` в режиме *lazy loading*, что позволяет эффективно обрабатывать большие массивы данных без избыточного потребления оперативной памяти.

---

### 🔹 Перечень исключений из модели

- `./get_db_vsh/dds_bfo_bal_fin_protect.csv`  
  Перечень предприятий по периодам, **исключаемых из обучающей выборки**, в связи с наличием формальных оснований отсутствия бухгалтерской отчётности.  
  Исключения формируются на основании требований действующего законодательства и используются для предотвращения искажения модели.

---

### 🔹 Формирование обучающей выборки для ML-моделей

- `./get_csv_data_for_ml/get_csv_for_ml.py`  
  Ключевой скрипт проекта, реализующий несколько логик одновременно:
  - извлечение финансовых показателей по каждому предприятию;
  - формирование экспертной отметки о признании предприятия *экономическим банкротом* в конкретном году;
  - построение временных последовательностей отчётности (лаги за 3 года);
  - отбор траекторий, потенциально ведущих к банкротству;
  - подготовка итогового обучающего датасета.

- `./get_csv_data_for_ml/csv_for_ml.csv`  
  Итоговая таблица признаков и целевой переменной для обучения ML-моделей.

- `./get_csv_data_for_ml/sql_query.sql`  
  SQL-скрипты формирования обучающей выборки с использованием DuckDB.

- `./get_csv_data_for_ml/export.sql`  
  SQL-скрипты, адаптированные для экспорта подготовленных данных в формат CSV.

---

### 🔹 Обучение ML-моделей

#### Скрипты обучения

- `./train_model_catboost.py` — обучение модели **CatBoost**
- `./train_model_lightgbm.py` — обучение модели **LightGBM**
- `./train_model_xgboost.py` — обучение модели **XGBoost**

#### Результаты обучения

- `./bankruptcy_catboost.cbm` — сохранённая модель CatBoost
- `./bankruptcy_lightgbm.txt` — сохранённая модель LightGBM
- `./bankruptcy_xgboost.json` — сохранённая модель XGBoost

---

### 🔹 Анализ результатов и визуализация

- `./apply_model_catboost_and_multifactor.py`  
  Анализ результатов моделирования, расчёт агрегированных метрик и интерпретация прогнозов.

- `./build_visuals_3_5.py`  
  Построение визуализаций для аналитических выводов и интерпретации качества моделей.

---

## 🧪 Для кого этот репозиторий

Проект может быть полезен:

- исследователям в области финансового анализа и риск-менеджмента;
- специалистам по ML / Data Science;
- аналитикам, работающим с бухгалтерской отчётностью;
- студентам и аспирантам (курсовые, ВКР, НИР);
- разработчикам, строящим end-to-end ML-пайплайны на табличных данных.

---

## ⚠️ Статус проекта

Проект находится в **исследовательской стадии**.  
Репозиторий не является production-решением, но демонстрирует архитектуру, методологию и практические приёмы, применимые в реальных аналитических системах.

---

## 📄 Лицензия

Использование кода допускается в исследовательских и учебных целях.  
При использовании в прикладных работах просьба указывать источник.
